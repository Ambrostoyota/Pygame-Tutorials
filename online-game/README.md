# 🎮 Pygame Online - Multiplayer Game

**跨平台在线多人游戏** - 支持 iOS、Android、Windows、Mac、Linux、Steam 等所有平台！

基于Pygame教程改造的网络版本，使用WebSocket实现实时多人对战。

## ✨ 特性

- 🌐 **真正的全平台支持** - 通过浏览器在任何设备运行
- 👥 **实时多人对战** - WebSocket低延迟同步
- 📱 **移动端优化** - 触控按钮支持iOS/Android
- 🎯 **房间系统** - 多个玩家同时在线
- ⚡ **自动同步** - 玩家位置、子弹、敌人实时更新
- 💯 **排行榜** - 实时显示所有玩家分数

## 🚀 快速开始

### 1. 安装依赖

```bash
cd online-game
npm install
```

### 2. 启动服务器

```bash
npm start
```

### 3. 开始游戏

在任何设备浏览器打开：
```
http://localhost:3000
```

#### 局域网多人游戏

1. 查看本机IP地址：
   ```bash
   # Mac/Linux
   ifconfig | grep "inet "
   
   # Windows
   ipconfig
   ```

2. 其他设备连接到：
   ```
   http://你的IP地址:3000
   ```
   例如：`http://192.168.1.100:3000`

## 🎮 游戏操作

### 键盘（电脑）
- **←/→** - 左右移动
- **↑** - 跳跃
- **空格** - 射击

### 触控（手机/平板）
- 使用屏幕底部的虚拟按钮控制

## 🌍 部署到云端（所有平台可访问）

### 部署到 Heroku

1. 安装 Heroku CLI
2. 创建应用：
   ```bash
   heroku create your-game-name
   ```

3. 推送代码：
   ```bash
   git push heroku master
   ```

4. 访问：`https://your-game-name.herokuapp.com`

### 部署到自己的服务器

```bash
# 安装 Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# 克隆项目
git clone <your-repo>
cd online-game

# 安装依赖
npm install

# 使用 PM2 保持运行
npm install -g pm2
pm2 start server.js --name "pygame-online"
pm2 save
pm2 startup
```

## 📱 各平台访问方式

### iOS
- Safari浏览器打开游戏地址
- 点击分享 → "添加到主屏幕" → 像原生APP一样使用

### Android
- Chrome浏览器打开游戏地址  
- 菜单 → "添加到主屏幕" → PWA应用

### Windows/Mac/Linux
- 任何现代浏览器直接访问

### Steam（可选）
使用 Electron 打包为桌面应用：
```bash
npm install electron --save-dev
# 配置 electron-builder
npm run build
```

## 🛠️ 技术栈

- **后端**: Node.js + Express + Socket.IO
- **前端**: HTML5 Canvas + Vanilla JavaScript
- **通信**: WebSocket (Socket.IO)
- **部署**: Docker / Heroku / VPS

## 📊 架构说明

```
服务器（server.js）
├── 玩家状态管理
├── 敌人生成与移动
├── 碰撞检测
└── 实时广播更新

客户端（index.html）
├── Canvas渲染
├── 输入处理（键盘/触控）
├── WebSocket通信
└── 本地预测（减少延迟）
```

## 🔧 配置

编辑 `server.js` 修改游戏参数：

```javascript
const GAME_WIDTH = 500;          // 游戏宽度
const GAME_HEIGHT = 480;         // 游戏高度
const ENEMY_RESPAWN_TIME = 5000; // 敌人生成间隔(ms)
const PORT = 3000;               // 服务器端口
```

## 🎯 未来计划

- [ ] 房间/频道系统
- [ ] 聊天功能
- [ ] 角色选择
- [ ] 更多武器类型
- [ ] 排行榜持久化
- [ ] 游戏回放系统

## 📝 开发说明

开发模式（自动重启）：
```bash
npm run dev
```

## 🤝 贡献

欢迎提交 Pull Request！

## 📄 许可证

MIT License

---

**享受游戏！** 🎮
